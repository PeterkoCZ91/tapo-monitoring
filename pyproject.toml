[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tapo-monitor"
version = "0.1.0"
description = "Comprehensive, config-driven monitoring stack for TP-Link Tapo PTZ cameras (detection, auto-tracking, AI descriptions, Telegram alerts)."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "pytapo",
    # pytapo's media_stream Downloader (SD-card clip download) imports aiofiles at
    # runtime but does not declare it — without it, sd_snapshot fails on a clean install.
    "aiofiles",
    "astral",
    "PyYAML",
]

[project.optional-dependencies]
dev = ["pytest", "ruff"]
scorer = ["onnxruntime", "numpy", "Pillow"]

[project.scripts]
tapo-monitor = "tapo_monitor.cli:main"

[tool.setuptools]
packages = ["tapo_monitor"]
py-modules = []

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# Tests set env vars / sys.path and use local imports before module import on purpose.
"tests/**" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
